{
  "name": "analytics",
  "version": "1.0.0",
  "dependencies": {
    "@supercharge/request-ip": "^1.2.0",
    "pg": "^8.10.0"
  },
  "scripts": {
    "build-start": "docker compose build && docker compose up",
    "add-current-db-to-build": "bash ./scripts/add-current-dump-to-build",
    "bring-down": "docker compose down"
  }
}
